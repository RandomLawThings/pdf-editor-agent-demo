# PDF Editor Agent - Environment Configuration
# Copy this file to .env and fill in your values

# ============================================================
# STANDALONE MODE (Recommended for local development)
# ============================================================
# Set to "true" to run without Manus platform
# This enables: local file storage, bypassed auth, Claude API
STANDALONE_MODE=true

# Your Anthropic API key (required for AI features in standalone mode)
# Get one at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# ============================================================
# OPTIONAL: Database (not required for standalone mode)
# ============================================================
# MySQL connection string (only needed if you want persistent storage)
# DATABASE_URL=mysql://user:password@localhost:3306/pdfeditor

# ============================================================
# MANUS PLATFORM MODE (ignore if using standalone)
# ============================================================
# These are automatically set when deployed on Manus platform
# VITE_APP_ID=
# JWT_SECRET=
# OAUTH_SERVER_URL=
# BUILT_IN_FORGE_API_URL=
# BUILT_IN_FORGE_API_KEY=
# OWNER_OPEN_ID=

# ============================================================
# Server Configuration
# ============================================================
PORT=3000
NODE_ENV=development
